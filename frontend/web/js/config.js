// Facilita trocar o endereço da API (local, OCI, etc.)
const API_BASE_URL = "http://localhost:8080/api/v1";

// IDs ou chaves podem ser salvos no localStorage
const STORAGE_KEYS = {
  CLIENTE_TIPO: "cliente_tipo",     // CLIENTE_COMPRADOR ou CLIENTE_VENDEDOR
  CLIENTE_ID: "cliente_id"
};
